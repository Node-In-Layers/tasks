import { Config, FeaturesContext } from '@node-in-layers/core'
import { MemoryServicesLayer, MemoryFeaturesLayer } from './types.js'

export const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: FeaturesContext<Config, MemoryServicesLayer, MemoryFeaturesLayer>
) => {
  return {}
}
