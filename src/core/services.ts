import { Config, ServicesContext } from '@node-in-layers/core'
import { CoreServices } from './types.js'

export const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: ServicesContext<Config>
): CoreServices => {
  return {}
}
